# KYA closeout - 2026-07-14

Session goal: make KYA one recording away from the OKX $100K submission, and turn a
real detector on honestly. Live URL untouched, listing untouched (#5290 under review).

## What shipped this session
1. **Verified the live headline is real.** Dry-ran the exact `/verify` the demo shows:
   Sentiment Oracle #3820 → BLOCK (44, signed) reproduces on the deployed service; full
   spread (Otto/Explorer SAFE, Scope/WhalePulse CAUTION) confirmed live + signatures verify
   offline against the pinned key. The demo headline survives a real call.
2. **Swept 82 real OKX marketplace agents through the live service** → 8 SAFE / 60 CAUTION /
   14 BLOCK, seeded into the live watchtower. Discriminating, not a rubber stamp.
3. **Found + fixed a real correctness bug the sweep surfaced.** KYA's domain-binding check
   compared the ERC-8004 `.well-known` agentId against the OKX marketplace id - different
   registries, different id-spaces - and falsely BLOCKed Barker Yield Agent (#2012, a real
   340-sale/4.91-rated agent) as "impersonation" because its Base id is #52838. Cross-registry
   ids are now excluded from comparison (not comparable ≠ guilty); same-namespace impostors
   still bite (FN-averse - Oscar's "false negatives are worse by far"). Barker → correctly SAFE.
   +3 regression tests, 103 green. Commit `49d68b2`.
4. **Rewrote `scripts/demo.sh`** into one self-verifying live walk (deployed URL, real
   responses, asserts verdicts, exit non-zero on divergence) + a fenced [CONTROLLED] section
   for the flip/poison detectors (real engine, labelled TEST agent - never sold as live).
   Runs green against live. Commit `a64ff89`.
5. **Wash gate**: confirmed no OKLink key present → correctly OFF. Wrote exact 2-step enable
   instructions incl. the buyer-vs-facilitator `from` check (`docs/WASH-GATE.md`).
6. **Test coverage**: mapped all 8 threat-model rows to tests - all covered; phishing/blacklist
   host is the thinnest (optional to strengthen).
7. **`SUBMISSION-CHECKLIST.md`** written (done / decisions / Oscar-only).

## Session update (later same day) - all of the above SHIPPED
- **Barker fix deployed** to production (SAFE 100 live).
- **Persistence added**: Railway volume `/data` + `KYA_DB_PATH`, watchtower survives redeploys.
- **Whole marketplace judged live**: `scripts/seed_all.py` swept all listed OKX agents (~363-371,
  enumeration varies) into the persistent store. Spread ~16 SAFE / ~325 CAUTION / 22 BLOCK.
- **Signing key rotated**: the key leaked into a session (`fabe730d…`) was still live; replaced
  with a fresh key (`2fbb9b67…`), redeployed, and re-swept so verdicts re-sign under it.
- **Submission kit refreshed** to current reality (371-agent proof, 103 tests); em dashes stripped.
- **Tweet drafted** (two hooks) pending Oscar's pick.

## Record & submit - the exact ~15-min human task (Oscar)
Everything below is pre-built; these are the only steps left.
1. **Dry-run** (warms the service): `bash scripts/demo.sh` - expect `DEMO GREEN`, exit 0. It is
   blip-proof (retries transient empties) and self-verifies every live verdict.
2. **Record** (~90s): run `bash scripts/demo.sh` and narrate from `demo-shotlist.md` (timed beats,
   exact lines). Two browser tabs: `/watchtower` and `/passport?agentId=3820`.
3. **X post**: use `draft-x-post.md` (recommended hook), attach the video, post in a morning slot
   (Tue-Thu 8-10am Paris). Glance at `/watchtower` first in case the 371/15/22 numbers shifted.
4. **Google form**: fill from `google-form-answers.md` (every field pre-filled with real values);
   paste the demo video link and the X post link. If it demands a source link, ping me to push
   the repo to GitHub.
5. **Submit.** Do not re-activate or change the listing.
   > 🚨 **CORRECTED Jul 15:** the original line here ("ASP #5290 approval is NOT required") was
   > **wrong**. Official rules Step 2: the ASP must pass internal review **and go live** to remain
   > eligible; if not approved or not live, the submission "will be deemed invalid." Approval is a
   > HARD gate. Verified Jul 15 07:23 UTC: `Listing under review` / `not listed`.

## Still waiting on OKX (WAS "not a blocker" — WRONG, it IS the eligibility blocker; corrected Jul 15)
- ASP #5290 listing approval. Trackable on request; if it lands it is a bonus, not a gate.

## Files for Oscar (repo root)
`draft-x-post.md` · `google-form-answers.md` · `demo-shotlist.md` · `SUBMISSION-CHECKLIST.md`

## Next session first-step
Confirm Railway volume/`KYA_DB_PATH` → deploy fix → re-run the 82-agent sweep to reseed →
record demo from `bash scripts/demo.sh`.

## V2 UPDATE (Jul 14-15) - built + deployed, all after "submission-ready"
Everything below shipped when Oscar pushed past "done" (see memory `feedback_ambition_past_done`).
- **`max_safe_usd` - priced-trust wedge (THE differentiator):** every verdict now carries a
  signed dollar ceiling (max single-tx to extend), earned from proven settled volume. WOLF $0,
  Otto $0.66, CoinAnk $35. Nobody else prices trust in dollars. Tagline: "Don't rate agents. Price them."
- **Tier-1 coverage:** domain-age (RDAP + CT first-seen), reviewer-concentration.
- **Adversarial verdict audit -> 4 false-positive classes fixed** (each verified on the real agent,
  regression-tested): RitMEX #2652 same-host-redirect false BLOCK; review_ring gated on sales
  (Newsliquid #2135, ThreeMeme); proven A2A agents = delivery proof (WorldCupCaller #1891, Arb Scout);
  wash-accusation removed without a cluster signal (CertiK). SAFE count 14->25 after fixes.
- **Deployed to live** (same URL), board re-swept + re-signed, demo.sh GREEN, **113 tests**.
- **Public repo synced** to V2 + `docs/THREAT-MODEL.md`: github.com/Morkeeth/kya-know-your-agent.
- **Roadmap upgraded** (vault `kya-moonshot-roadmap-2026-07-14.md`): the reframe is KYA-as-UNDERWRITER
  (stake/reputation-sized dollar ceiling), on RFC 9421 identity + ERC-8004 inputs, flagship moonshot =
  x402 escrow-sized-to-trust (advisory -> enforcing). Grounded in 6 research passes (frontier + competitive).
- Deadline Jul 17 unchanged. Oscar records the V2 app; task still ~15 min (record + post + form + submit).
